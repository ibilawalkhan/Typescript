var myFavoriteTranspotation = ["Cars", "Jets", "Planes", "Boats"];
for (var i = 0; i < myFavoriteTranspotation.length; i++) {
    console.log("I will love to own ".concat(myFavoriteTranspotation[i]));
}
